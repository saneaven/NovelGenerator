Generic single-database configuration for Alembic migrations.

This directory contains database migration scripts for the Novel Buds application.

Fresh installs are built from a single baseline revision (`0001_baseline`). Add new revisions only when the schema changes after that baseline.

## Common Commands

# Create a new migration (auto-generate from model changes)
alembic revision --autogenerate -m "description of changes"

# Create a blank migration
alembic revision -m "description"

# Apply all pending migrations
alembic upgrade head

# Downgrade one revision
alembic downgrade -1

# Show current revision
alembic current

# Show migration history
alembic history

# Downgrade to a specific revision
alembic downgrade <revision_id>
