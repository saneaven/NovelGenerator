# Production (VPS)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me-prod
POSTGRES_DB=novel_generator

# Backend secrets (MUST be stable; changing them breaks sessions/credential decryption)
JWT_SECRET_KEY=change-me-prod
CREDENTIALS_ENCRYPTION_KEY=change-me-prod

# Frontend
VITE_API_URL=
